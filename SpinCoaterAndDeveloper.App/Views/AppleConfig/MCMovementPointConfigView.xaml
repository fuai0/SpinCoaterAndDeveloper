<UserControl
    x:Class="SpinCoaterAndDeveloper.App.Views.MCMovementPointConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:jogDirType="clr-namespace:MotionCardServiceInterface;assembly=MotionCardServiceInterface"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:movementnType="clr-namespace:SpinCoaterAndDeveloper.Shared.Models.MotionControlModels;assembly=SpinCoaterAndDeveloper.Shared"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:prismEx="clr-namespace:PrismAutomationPlatformExt;assembly=PrismAutomationPlatformExt"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:vc="clr-namespace:ValueConverters;assembly=ValueConverters"
    xmlns:viewmodels="clr-namespace:SpinCoaterAndDeveloper.App.ViewModels"
    xmlns:zc="clr-namespace:ZeroControls;assembly=ZeroControls"
    d:DataContext="{d:DesignInstance Type=viewmodels:MCMovementPointConfigViewModel}"
    d:DesignHeight="700"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <prismEx:BindingProxy x:Key="BindingProxy" Data="{Binding}" />
        <vc:BoolToVisibilityConverter
            x:Key="BoolToVisibilityConverter"
            FalseValue="Collapsed"
            TrueValue="Visible" />
        <ObjectDataProvider
            x:Key="MovementType"
            MethodName="GetValues"
            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="movementnType:MovementType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider
            x:Key="MovementJogArrivedType"
            MethodName="GetValues"
            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="movementnType:JogArrivedType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider
            x:Key="JogDirType"
            MethodName="GetValues"
            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="jogDirType:Direction" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <vc:ValueConverterGroup x:Key="MovementTypeToVisible">
            <vc:EnumToBoolConverter />
            <vc:BoolToVisibilityConverter FalseValue="Hidden" TrueValue="Visible" />
        </vc:ValueConverterGroup>
        <Style x:Key="GridShowAllAxesStyle" TargetType="Grid">
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=ShowAllAxesCheckBox, Path=IsChecked}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Visibility" Value="Visible" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=ShowAllAxesCheckBox, Path=IsChecked}" Value="False" />
                        <Condition Binding="{Binding InvolveAxis}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Visibility" Value="Visible" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=ShowAllAxesCheckBox, Path=IsChecked}" Value="False" />
                        <Condition Binding="{Binding InvolveAxis}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Visibility" Value="Collapsed" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        <vc:EnumToObjectConverter x:Key="EnumToPositionConverter">
            <vc:EnumToObjectConverter.Items>
                <ResourceDictionary>
                    <sys:String x:Key="LineAxis">mm</sys:String>
                    <sys:String x:Key="RotateAxis">°(度)</sys:String>
                </ResourceDictionary>
            </vc:EnumToObjectConverter.Items>
        </vc:EnumToObjectConverter>
        <vc:EnumToObjectConverter x:Key="EnumToAccConverter">
            <vc:EnumToObjectConverter.Items>
                <ResourceDictionary>
                    <sys:String x:Key="LineAxis">mm/s²</sys:String>
                    <sys:String x:Key="RotateAxis">°/s²</sys:String>
                </ResourceDictionary>
            </vc:EnumToObjectConverter.Items>
        </vc:EnumToObjectConverter>
        <vc:EnumToObjectConverter x:Key="EnumToVelConverter">
            <vc:EnumToObjectConverter.Items>
                <ResourceDictionary>
                    <sys:String x:Key="LineAxis">mm/s</sys:String>
                    <sys:String x:Key="RotateAxis">°/s</sys:String>
                </ResourceDictionary>
            </vc:EnumToObjectConverter.Items>
        </vc:EnumToObjectConverter>
        <vc:ValueConverterGroup x:Key="SecurityStepCountsToForeground">
            <vc:IntegerToBoolConverter BaseOnFalseValue="True" FalseValue="0" />
            <vc:BoolToBrushConverter FalseValue="{StaticResource SecondaryAccentForegroundBrush}" TrueValue="{StaticResource SecondaryAccentBrush}" />
        </vc:ValueConverterGroup>
    </UserControl.Resources>
    <materialDesign:DialogHost>
        <materialDesign:DrawerHost IsRightDrawerOpen="{Binding IsRightDrawerOpen}">
            <materialDesign:DrawerHost.RightDrawerContent>
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <DockPanel
                        Width="300"
                        Margin="10,10"
                        VerticalAlignment="Top"
                        LastChildFill="False">
                        <DockPanel.Resources>
                            <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="{x:Type TextBox}">
                                <Setter Property="Margin" Value="5,5,5,0" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="5,5,5,0" />
                            </Style>
                        </DockPanel.Resources>
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Name}" />
                        <TextBox
                            materialDesign:HintAssist.Hint="{DynamicResource MustAndUniqueHint}"
                            DockPanel.Dock="Top"
                            Text="{Binding NewMovementPointName.Name}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource ConfigAxisInputCNName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewMovementPointName.CNName}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource ConfigAxisInputENName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewMovementPointName.ENName}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource ConfigAxisInputVNName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewMovementPointName.VNName}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Group}" />
                        <ComboBox
                            Margin="5,5,5,0"
                            DockPanel.Dock="Top"
                            IsEditable="True"
                            ItemsSource="{Binding MovementPointGroupCollection}"
                            Text="{Binding NewMovementPointName.Group}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Remark}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewMovementPointName.Backup}" />
                        <Button
                            Margin="5,5,5,0"
                            Command="{Binding AddNewMovementPointCfgCommand}"
                            Content="{DynamicResource AddNewSpacePoint}"
                            DockPanel.Dock="Top" />
                    </DockPanel>
                </ScrollViewer>
            </materialDesign:DrawerHost.RightDrawerContent>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>
                <Grid Margin="0,0,2,0" Background="{StaticResource AppleHeavyBackGroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ListBox
                        DisplayMemberPath="Name"
                        IsEnabled="{Binding ButtonEnable}"
                        ItemsSource="{Binding MovementPointCollection}"
                        SelectedItem="{Binding CurrentSelectPointName}" />
                    <StackPanel Grid.Row="1" Margin="0,4,0,0">
                        <UniformGrid Margin="4,0,4,0" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding MoveToSelectedPointCommand}"
                                Content="{DynamicResource MoveToPointPosition}" />
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding StopMoveToSelectedCommand}"
                                Content="{DynamicResource StopMoveToPointPosition}" />
                        </UniformGrid>
                        <Grid Margin="4,8,4,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="5*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontSize="14"
                                Text="{DynamicResource GlobalVelocity}" />
                            <zc:NumericUpDown
                                Grid.Column="1"
                                Margin="2,0,0,0"
                                VerticalAlignment="Bottom"
                                HorizontalContentAlignment="Center"
                                BorderThickness="0,0,0,1"
                                CornerRadius="0"
                                Increment="1"
                                Maximum="100"
                                Minimum="1"
                                Style="{StaticResource ZeroNumericUpDown}"
                                Value="{Binding GlobalVel}" />
                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontSize="18"
                                Text="%" />
                            <Button
                                Grid.Column="3"
                                Margin="2,0,4,0"
                                Padding="0"
                                VerticalAlignment="Bottom"
                                Command="{Binding SaveGlobalVelCommand}"
                                Content="{DynamicResource Save}" />
                        </Grid>
                        <Grid Margin="4,4,4,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="5*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Margin="0,0,2,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontSize="14"
                                Text="{DynamicResource SelectGroup}" />
                            <ComboBox
                                x:Name="MCSelect"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                Margin="2,0,4,0"
                                HorizontalContentAlignment="Center"
                                ItemsSource="{Binding MovementPointGroupCollection}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding MovementPointGourpChangedCommand}" CommandParameter="{Binding SelectedItem, ElementName=MCSelect}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ComboBox>
                        </Grid>
                        <UniformGrid Margin="4,8,4,0" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding ShowAllMovementPointCommand}"
                                Content="{DynamicResource ShowAllMCPoints}" />
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding AddNewMovementPointCommand}"
                                Content="{DynamicResource AddNewSpacePoint}" />
                        </UniformGrid>
                        <UniformGrid Margin="4,8,4,0" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding DeleteSelectedMovementPointCommand}"
                                Content="{DynamicResource DeleteMotionPoint}"
                                Foreground="{StaticResource SecondaryAccentForegroundBrush}" />
                        </UniformGrid>
                        <UniformGrid Margin="4,8,4,8" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding SaveSelectedMovementPointCommand}"
                                Content="{DynamicResource SaveCheck}" />
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding SaveAllMovementPointCommand}"
                                Content="{DynamicResource SaveAll}" />
                        </UniformGrid>

                    </StackPanel>
                </Grid>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="9*" />
                        <RowDefinition Height="11*" />
                        <RowDefinition Height="10*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Grid Margin="2,0,2,2" Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Grid.Resources>
                            <Style BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="TextBlock">
                                <Setter Property="Margin" Value="0,0,5,0" />
                                <Setter Property="HorizontalAlignment" Value="Right" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                            <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                                <Setter Property="Margin" Value="5,0,15,0" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Block.TextAlignment" Value="Center" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                        </Grid.Resources>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Text="{DynamicResource Name}" />
                        <TextBox
                            Grid.Row="0"
                            Grid.Column="1"
                            Text="{Binding CurrentSelectPointName.Name}" />
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="{DynamicResource VNName}" />
                        <TextBox
                            Grid.Row="0"
                            Grid.Column="3"
                            Text="{Binding CurrentSelectPointName.VNName}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Text="{DynamicResource CNName}" />
                        <TextBox
                            Grid.Row="1"
                            Grid.Column="1"
                            Text="{Binding CurrentSelectPointName.CNName}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="{DynamicResource Group}" />
                        <ComboBox
                            Grid.Row="1"
                            Grid.Column="3"
                            Margin="5,0,15,0"
                            HorizontalContentAlignment="Center"
                            IsEditable="True"
                            ItemsSource="{Binding MovementPointGroupCollection}"
                            Text="{Binding CurrentSelectPointName.Group}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Text="{DynamicResource ENName}" />
                        <TextBox
                            Grid.Row="2"
                            Grid.Column="1"
                            Text="{Binding CurrentSelectPointName.ENName}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="{DynamicResource Remark}" />
                        <TextBox
                            Grid.Row="2"
                            Grid.Column="3"
                            Text="{Binding CurrentSelectPointName.Backup}" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Text="{DynamicResource ManualPointMotionTimedOut}" />
                        <TextBox
                            Grid.Row="3"
                            Grid.Column="1"
                            Text="{Binding CurrentSelectPointName.ManualMoveSecurityTimeOut}" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="2"
                            HorizontalAlignment="Left"
                            Text="ms" />
                    </Grid>

                    <Border
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="2,0,0,2"
                        Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Border
                            Margin="4"
                            BorderBrush="{StaticResource ApplelHeavyTextForegroundBrush}"
                            BorderThickness="1"
                            CornerRadius="5">
                            <ListBox DockPanel.Dock="Right" ItemsSource="{Binding MessageList}">
                                <ListBox.ContextMenu>
                                    <ContextMenu DataContext="{Binding Data, Source={StaticResource BindingProxy}}">
                                        <MenuItem
                                            Command="{Binding MsgClearCommand}"
                                            Header="{DynamicResource ClearLog}"
                                            Icon="{materialDesign:PackIcon Kind=Close}" />
                                        <MenuItem
                                            Command="{Binding OpenLogDirectoryCommand}"
                                            Header="{DynamicResource OpenLog}"
                                            Icon="{materialDesign:PackIcon Kind=FolderSearchOutline}" />
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                                <i:Interaction.Behaviors>
                                    <prismEx:ListBoxScrollToBottomExtensions />
                                </i:Interaction.Behaviors>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Time, StringFormat={}{0:HH:mm:ss:}}" />
                                            <TextBlock
                                                Margin="5,0,0,0"
                                                Text="{Binding Text}"
                                                TextWrapping="Wrap" />
                                        </DockPanel>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="INFO">
                                                <Setter Property="ListBoxItem.Foreground" Value="Black" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="DEBUG">
                                                <Setter Property="ListBoxItem.Foreground" Value="Black" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="WARN">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageOrangeBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="ERROR">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageRedBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="FATAL">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageRedBrush}" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Border>

                    <Grid
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="2,2,0,2"
                        Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                Margin="5,0"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{DynamicResource MotionPointsParticipateAxis}" />
                            <ItemsControl
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding CurrentSelectPointName.MovementPointPositionsCollection}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel VerticalAlignment="Center" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox
                                            Margin="5,0"
                                            VerticalAlignment="Center"
                                            Content="{Binding AxisInfo.Name}"
                                            IsChecked="{Binding InvolveAxis}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <CheckBox
                                x:Name="ManualMPSecurityCfg"
                                Grid.Column="2"
                                IsChecked="{Binding CurrentSelectPointName.ManualMoveSecurityEnable}" />
                            <Button
                                Grid.Column="3"
                                Height="20"
                                Margin="2,2,2,2"
                                Padding="10,0,10,0"
                                Command="{Binding MovementPointSecurityCfgCommand}"
                                Content="{DynamicResource ManualClickSecurityConfiguration}"
                                IsEnabled="{Binding ElementName=ManualMPSecurityCfg, Path=IsChecked}" />
                            <TextBlock
                                Grid.Column="4"
                                VerticalAlignment="Center"
                                FontSize="16"
                                FontWeight="Bold"
                                Foreground="{Binding CurrentSelectPointName.MovementPointSecuritiesCollection.Count, Converter={StaticResource SecurityStepCountsToForeground}, ConverterParameter=0}"
                                Text="{Binding CurrentSelectPointName.MovementPointSecuritiesCollection.Count}"
                                Visibility="{Binding ElementName=ManualMPSecurityCfg, Converter={StaticResource BoolToVisibilityConverter}, Path=IsChecked}" />
                            <Button
                                Grid.Column="5"
                                Height="20"
                                Margin="2,2,5,2"
                                Padding="10,0,10,0"
                                Command="{Binding TeachPointCommand}"
                                Content="{DynamicResource Teach}" />
                        </Grid>
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding CurrentSelectPointName.MovementPointPositionsCollection}">
                            <ItemsControl.Template>
                                <ControlTemplate>
                                    <ScrollViewer
                                        CanContentScroll="False"
                                        HorizontalScrollBarVisibility="Auto"
                                        VerticalScrollBarVisibility="Hidden">
                                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                                    </ScrollViewer>
                                </ControlTemplate>
                            </ItemsControl.Template>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid
                                        Width="328"
                                        Margin="5,0,0,0"
                                        Visibility="{Binding InvolveAxis, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Grid.Resources>
                                            <Style BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="TextBlock">
                                                <Setter Property="Margin" Value="0,0,2,0" />
                                                <Setter Property="HorizontalAlignment" Value="Left" />
                                                <Setter Property="VerticalAlignment" Value="Center" />
                                            </Style>
                                            <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                                                <Setter Property="Margin" Value="2,0,2,0" />
                                                <Setter Property="VerticalAlignment" Value="Center" />
                                                <Setter Property="Block.TextAlignment" Value="Center" />
                                                <Setter Property="Padding" Value="0" />
                                            </Style>
                                        </Grid.Resources>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Border
                                            Grid.Row="0"
                                            Grid.RowSpan="6"
                                            Grid.Column="4"
                                            Margin="5,10,0,3"
                                            BorderBrush="#939393"
                                            BorderThickness="0,0,1,0" />
                                        <TextBlock
                                            Grid.Row="0"
                                            Grid.Column="0"
                                            Grid.ColumnSpan="3"
                                            HorizontalAlignment="Left"
                                            FontSize="17"
                                            FontWeight="Bold"
                                            Text="{Binding AxisInfo.Name}" />
                                        <TextBlock
                                            Grid.Row="1"
                                            Grid.Column="0"
                                            Text="{DynamicResource MoveType}" />
                                        <ComboBox
                                            Grid.Row="1"
                                            Grid.Column="1"
                                            Height="20"
                                            Margin="2,0,2,0"
                                            Padding="0"
                                            VerticalAlignment="Center"
                                            HorizontalContentAlignment="Center"
                                            ItemsSource="{Binding Source={StaticResource MovementType}}"
                                            SelectedItem="{Binding MovementPointType}" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="0"
                                            Text="{DynamicResource AbsoluteDisplacement}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Abs}" />
                                        <TextBox
                                            Grid.Row="2"
                                            Grid.Column="1"
                                            Grid.ColumnSpan="2"
                                            Text="{Binding AbsValue}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Abs}" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="3"
                                            Margin="5,0,0,0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToPositionConverter}}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Abs}" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="0"
                                            Text="{DynamicResource RelativeDisplacement}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Rel}" />
                                        <TextBox
                                            Grid.Row="2"
                                            Grid.Column="1"
                                            Grid.ColumnSpan="2"
                                            Text="{Binding RelValue}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Rel}" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="3"
                                            Margin="5,0,0,0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToPositionConverter}}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Rel}" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="0"
                                            Text="{DynamicResource InPlaceIOName}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Jog}" />
                                        <ComboBox
                                            Grid.Row="2"
                                            Grid.Column="1"
                                            Height="20"
                                            Margin="2,0,2,0"
                                            Padding="0"
                                            HorizontalContentAlignment="Left"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding DataContext.IOInputCollection, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                            SelectedItem="{Binding JogIOInputInfo}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Jog}" />
                                        <ComboBox
                                            Grid.Row="2"
                                            Grid.Column="2"
                                            Height="20"
                                            Margin="2,0,2,0"
                                            Padding="0"
                                            HorizontalContentAlignment="Left"
                                            ItemsSource="{Binding Source={StaticResource JogDirType}}"
                                            SelectedItem="{Binding JogDirection}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Jog}" />
                                        <ComboBox
                                            Grid.Row="2"
                                            Grid.Column="3"
                                            Height="20"
                                            Margin="5,0,0,0"
                                            Padding="0"
                                            VerticalAlignment="Center"
                                            HorizontalContentAlignment="Left"
                                            ItemsSource="{Binding Source={StaticResource MovementJogArrivedType}}"
                                            SelectedItem="{Binding JogArrivedCondition}"
                                            Visibility="{Binding MovementPointType, Converter={StaticResource MovementTypeToVisible}, ConverterParameter=Jog}" />
                                        <TextBlock
                                            Grid.Row="3"
                                            Grid.Column="0"
                                            Text="{DynamicResource Speed}" />
                                        <TextBox
                                            Grid.Row="3"
                                            Grid.Column="1"
                                            Grid.ColumnSpan="2"
                                            Text="{Binding Vel}" />
                                        <TextBlock
                                            Grid.Row="3"
                                            Grid.Column="3"
                                            Margin="5,0,0,0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToVelConverter}}" />
                                        <TextBlock
                                            Grid.Row="4"
                                            Grid.Column="0"
                                            Text="{DynamicResource Acceleration}" />
                                        <TextBox
                                            Grid.Row="4"
                                            Grid.Column="1"
                                            Grid.ColumnSpan="2"
                                            Text="{Binding Acc}" />
                                        <TextBlock
                                            Grid.Row="4"
                                            Grid.Column="3"
                                            Margin="5,0,0,0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToAccConverter}}" />
                                        <TextBlock
                                            Grid.Row="5"
                                            Grid.Column="0"
                                            Text="{DynamicResource Deceleration}" />
                                        <TextBox
                                            Grid.Row="5"
                                            Grid.Column="1"
                                            Grid.ColumnSpan="2"
                                            Text="{Binding Dec}" />
                                        <TextBlock
                                            Grid.Row="5"
                                            Grid.Column="3"
                                            Margin="5,0,0,0"
                                            HorizontalAlignment="Right"
                                            Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToAccConverter}}" />
                                        <!--<TextBlock Text="偏移" />
                                        <TextBox Text="{Binding Offset}" />-->
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>

                    <Grid
                        Grid.Row="2"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="2,2,0,0"
                        Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <CheckBox
                                x:Name="ShowAllAxesCheckBox"
                                Margin="4,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{DynamicResource ShowsAllAxisOperations}" />
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding CurrentSelectPointName.MovementPointPositionsCollection}">
                                <ItemsControl.Template>
                                    <ControlTemplate>
                                        <ScrollViewer
                                            CanContentScroll="False"
                                            HorizontalScrollBarVisibility="Auto"
                                            VerticalScrollBarVisibility="Hidden">
                                            <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </ItemsControl.Template>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid
                                            Width="328"
                                            Margin="5,0,0,0"
                                            Style="{StaticResource GridShowAllAxesStyle}">
                                            <Grid.Resources>
                                                <Style BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                </Style>
                                                <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                    <Setter Property="Block.TextAlignment" Value="Center" />
                                                    <Setter Property="Padding" Value="0" />
                                                </Style>
                                            </Grid.Resources>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Border
                                                Grid.Row="0"
                                                Grid.RowSpan="5"
                                                Grid.Column="4"
                                                Margin="5,10,0,3"
                                                BorderBrush="#939393"
                                                BorderThickness="0,0,1,0" />
                                            <TextBlock
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Grid.ColumnSpan="3"
                                                Margin="0"
                                                Padding="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                FontSize="17"
                                                FontWeight="Bold"
                                                Text="{Binding AxisInfo.Name}" />
                                            <Button
                                                Grid.Row="0"
                                                Grid.Column="3"
                                                Margin="2,0,0,2"
                                                Padding="0"
                                                Command="{Binding DataContext.AxisClearErrorCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding AxisInfo}"
                                                Content="{DynamicResource ClearErrors}" />
                                            <TextBlock
                                                Grid.Row="1"
                                                Grid.Column="0"
                                                Padding="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                Text="{DynamicResource Dot/inchSpeed}" />
                                            <zc:NumericUpDown
                                                Grid.Row="2"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                HorizontalContentAlignment="Center"
                                                BorderThickness="0,0,0,1"
                                                CornerRadius="0"
                                                DecimalPlaces="2"
                                                Increment="1"
                                                Minimum="1"
                                                Style="{StaticResource ZeroNumericUpDown}"
                                                Value="{Binding AxisInfo.JogVel}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="LostFocus">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.UpdateJogVelCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" CommandParameter="{Binding AxisInfo}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </zc:NumericUpDown>
                                            <TextBlock
                                                Grid.Row="2"
                                                Grid.Column="1"
                                                Margin="2"
                                                HorizontalAlignment="Left"
                                                Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToVelConverter}}" />
                                            <Button
                                                Grid.Row="1"
                                                Grid.Column="2"
                                                Margin="0,0,2,2"
                                                Content="{materialDesign:PackIcon Kind=ChevronTripleLeft,
                                                                                  Size=26}"
                                                IsEnabled="{Binding DataContext.ButtonEnable, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.JogNegativeCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" CommandParameter="{Binding AxisInfo}" />
                                                    </i:EventTrigger>
                                                    <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.JogMoveStopCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" CommandParameter="{Binding AxisInfo}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </Button>
                                            <Button
                                                Grid.Row="1"
                                                Grid.Column="3"
                                                Margin="2,0,0,2"
                                                Content="{materialDesign:PackIcon Kind=ChevronTripleRight,
                                                                                  Size=26}"
                                                IsEnabled="{Binding DataContext.ButtonEnable, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.JogPositiveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" CommandParameter="{Binding AxisInfo}" />
                                                    </i:EventTrigger>
                                                    <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.JogMoveStopCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" CommandParameter="{Binding AxisInfo}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </Button>
                                            <Button
                                                Grid.Row="2"
                                                Grid.Column="2"
                                                Margin="0,2,2,0"
                                                Padding="0"
                                                Command="{Binding DataContext.AxisServoOnCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding AxisInfo}"
                                                Content="{DynamicResource UpperEnabl}" />
                                            <Button
                                                Grid.Row="2"
                                                Grid.Column="3"
                                                Margin="2,2,0,0"
                                                Padding="0"
                                                Command="{Binding DataContext.AxisServoOffCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding AxisInfo}"
                                                Content="{DynamicResource DownEnabl}" />
                                            <TextBlock
                                                Grid.Row="3"
                                                Grid.Column="0"
                                                Margin="0"
                                                Padding="0"
                                                HorizontalAlignment="Left"
                                                Text="{DynamicResource StepMoveInfo}" />
                                            <zc:NumericUpDown
                                                Grid.Row="4"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                HorizontalContentAlignment="Center"
                                                BorderThickness="0,0,0,1"
                                                CornerRadius="0"
                                                DecimalPlaces="3"
                                                Increment="0.01"
                                                Minimum="0.001"
                                                Style="{StaticResource ZeroNumericUpDown}"
                                                Value="{Binding AxisInfo.RelMoveDistance}" />
                                            <TextBlock
                                                Grid.Row="4"
                                                Grid.Column="1"
                                                Margin="2"
                                                HorizontalAlignment="Left"
                                                Text="{Binding AxisInfo.Type, Converter={StaticResource EnumToPositionConverter}}" />
                                            <Button
                                                Grid.Row="4"
                                                Grid.Column="2"
                                                Margin="0,0,2,0"
                                                Padding="0"
                                                Command="{Binding DataContext.RelMoveCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding AxisInfo}"
                                                Content="{DynamicResource StepMoveStartButton}" />
                                            <Button
                                                Grid.Row="4"
                                                Grid.Column="3"
                                                Margin="2,0,0,0"
                                                Padding="0"
                                                Command="{Binding DataContext.RelMoveStopCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding AxisInfo}"
                                                Content="{DynamicResource StepMoveStopButton}" />

                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</UserControl>
