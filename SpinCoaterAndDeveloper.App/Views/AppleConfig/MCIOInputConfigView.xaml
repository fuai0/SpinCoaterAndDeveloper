<UserControl
    x:Class="SpinCoaterAndDeveloper.App.Views.MCIOInputConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:bp="clr-namespace:PrismAutomationPlatformExt;assembly=PrismAutomationPlatformExt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:listboxEx="clr-namespace:PrismAutomationPlatformExt;assembly=PrismAutomationPlatformExt"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vc="clr-namespace:ValueConverters;assembly=ValueConverters"
    xmlns:viewmodels="clr-namespace:SpinCoaterAndDeveloper.App.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MCIOInputConfigViewModel}"
    d:DesignHeight="700"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <bp:BindingProxy x:Key="BindingProxy" Data="{Binding}" />
        <vc:BoolToStringConverter
            x:Key="BoolToStringConverter"
            FalseValue="Disable"
            TrueValue="Enable" />
    </UserControl.Resources>
    <materialDesign:DialogHost>
        <materialDesign:DrawerHost IsRightDrawerOpen="{Binding IsRightDrawerOpen}">
            <materialDesign:DrawerHost.RightDrawerContent>
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <DockPanel
                        Width="300"
                        Margin="10,0"
                        VerticalAlignment="Top"
                        LastChildFill="False">
                        <DockPanel.Resources>
                            <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="{x:Type TextBox}">
                                <Setter Property="Margin" Value="5,5,5,0" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="5,5,5,0" />
                            </Style>
                        </DockPanel.Resources>
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource EnterInputName}" />
                        <TextBox
                            materialDesign:HintAssist.Hint="{DynamicResource MustAndUniqueHint}"
                            DockPanel.Dock="Top"
                            Text="{Binding NewIOInput.Name}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Number}" />
                        <TextBox
                            materialDesign:HintAssist.Hint="{DynamicResource MustAndUniqueHint}"
                            DockPanel.Dock="Top"
                            Text="{Binding NewIOInput.Number}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource EnterCNName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewIOInput.CNName}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource EnterENName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewIOInput.ENName}" />
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource EnterVNName}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewIOInput.VNName}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource InputIOHardwareLocation}" />
                        <TextBox DockPanel.Dock="Top" Text="{Binding NewIOInput.PhysicalLocation}" />

                        <CheckBox
                            Margin="4,5"
                            Content="{DynamicResource InputIfReverse}"
                            DockPanel.Dock="Top"
                            IsChecked="{Binding NewIOInput.ReverseEnable}" />

                        <CheckBox
                            x:Name="InputShieldEnable"
                            Margin="4,5"
                            Content="{DynamicResource InputIfValid}"
                            DockPanel.Dock="Top"
                            IsChecked="{Binding NewIOInput.ShieldEnable}" />


                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource InputShieldDefaultValue}" />
                        <CheckBox
                            Margin="4,5"
                            Content="{DynamicResource ShieldDefaultValueInfo}"
                            DockPanel.Dock="Top"
                            IsChecked="{Binding NewIOInput.ShiedlEnableDefaultValue}"
                            IsEnabled="{Binding ElementName=InputShieldEnable, Path=IsChecked}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Group}" />
                        <ComboBox
                            Margin="0,5"
                            DockPanel.Dock="Top"
                            IsEditable="True"
                            ItemsSource="{Binding IOInputGroupCollection}"
                            Text="{Binding NewIOInput.Group}" />

                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Backup}" />
                        <TextBox
                            Margin="0,5"
                            DockPanel.Dock="Top"
                            Text="{Binding NewIOInput.Backup}" />
                        <Button
                            Margin="0,5"
                            Command="{Binding AddInputCommand}"
                            Content="{DynamicResource InsertInputButton}"
                            DockPanel.Dock="Top" />
                    </DockPanel>
                </ScrollViewer>
            </materialDesign:DrawerHost.RightDrawerContent>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>
                <Grid Margin="0,0,2,0" Background="{StaticResource AppleHeavyBackGroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ListBox
                        DisplayMemberPath="Name"
                        ItemsSource="{Binding IOInputCfgCollections}"
                        SelectedItem="{Binding CurrentSelectIOInput}" />
                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                        <Grid Margin="4,0,4,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="4*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Margin="0,0,2,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{DynamicResource FilterGroup}" />
                            <ComboBox
                                x:Name="GroupSelect"
                                Grid.Column="1"
                                Margin="2,0,0,0"
                                Padding="0"
                                HorizontalContentAlignment="Center"
                                ItemsSource="{Binding IOInputGroupCollection}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding GroupChangedCommand}" CommandParameter="{Binding ElementName=GroupSelect, Path=SelectedValue}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ComboBox>
                        </Grid>
                        <UniformGrid Margin="4,8,4,0" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding ShowAllInputsCommand}"
                                Content="{DynamicResource ShowAllInputs}" />
                            <Button
                                Margin="2,0,0,0"
                                Padding="0"
                                Command="{Binding AddIOCfgCommand}"
                                Content="{DynamicResource AddIO}" />
                        </UniformGrid>
                        <UniformGrid Margin="4,8,4,0" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding DeleteLastInputCommand}"
                                Content="{DynamicResource DeleteLastInputButton}"
                                Foreground="{StaticResource SecondaryAccentForegroundBrush}" />
                        </UniformGrid>
                        <UniformGrid Margin="4,8,4,8" Columns="2">
                            <Button
                                Margin="0,0,2,0"
                                Padding="0"
                                Command="{Binding SaveSelectedIOInputCommand}"
                                Content="{DynamicResource SaveCheck}" />
                            <Button
                                Margin="2,0,0,0"
                                Padding="0"
                                Command="{Binding SaveAllIOInputsCommand}"
                                Content="{DynamicResource Save}" />
                        </UniformGrid>
                    </StackPanel>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Border
                        Grid.Row="0"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="2,0,0,2"
                        Background="{StaticResource AppleHeavyBackGroundBrush}"
                        CornerRadius="0">
                        <DataGrid
                            materialDesign:DataGridAssist.CellPadding="10 2 10 2"
                            materialDesign:DataGridAssist.ColumnHeaderPadding="10 2 10 2"
                            materialDesign:DataGridAssist.SelectedCellBorderBrush="Transparent"
                            AutoGenerateColumns="False"
                            Background="{StaticResource AppleHeavyBackGroundBrush}"
                            CanUserAddRows="False"
                            CanUserSortColumns="False"
                            HeadersVisibility="All"
                            IsReadOnly="True"
                            ItemsSource="{Binding IOInputCfgCollections}"
                            RowHeaderWidth="1"
                            ScrollViewer.HorizontalScrollBarVisibility="Visible"
                            ScrollViewer.VerticalScrollBarVisibility="Visible"
                            SelectedItem="{Binding CurrentSelectIOInput}"
                            SelectionMode="Single">
                            <DataGrid.Resources>
                                <Style
                                    x:Key="DataGridColumnElementStyleDefine"
                                    BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                    TargetType="{x:Type TextBlock}">
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Binding="{Binding Number, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource Number}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding Name, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource Name}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding CNName, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource CNName}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding ENName, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource ENName}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding VNName, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource VNName}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding PhysicalLocation, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource PhysicalLocation}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn
                                    Binding="{Binding ReverseEnable, Mode=OneWay, Converter={StaticResource BoolToStringConverter}}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource ReverseEnable}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.CellStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ReverseEnable}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ReverseEnable}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentForegroundBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.CellStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn
                                    Binding="{Binding ShieldEnable, Mode=OneWay, Converter={StaticResource BoolToStringConverter}}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource ShieldEnable}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.CellStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ShieldEnable}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ShieldEnable}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentForegroundBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.CellStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn
                                    Binding="{Binding ShiedlEnableDefaultValue, Mode=OneWay}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource ShieldEnableDefaultValue}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.CellStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ShiedlEnableDefaultValue}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ShiedlEnableDefaultValue}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryAccentForegroundBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.CellStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn
                                    Binding="{Binding Group, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource Group}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding Backup, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="{DynamicResource Backup}" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Binding="{Binding Tag, Mode=OneWay}"
                                    EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}"
                                    ElementStyle="{StaticResource DataGridColumnElementStyleDefine}">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                            <Setter Property="Content" Value="Tag" />
                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    <ScrollViewer
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="2,2,2,0"
                        Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Grid>
                            <Grid.Resources>
                                <Style BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,0,5,0" />
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                                <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                                    <Setter Property="Margin" Value="5,0,0,0" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Block.TextAlignment" Value="Center" />
                                    <Setter Property="Padding" Value="0" />
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="4*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="{DynamicResource Number}" />
                            <TextBox
                                Grid.Row="0"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.Number, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="{DynamicResource Name}" />
                            <TextBox
                                Grid.Row="1"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.Name, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="{DynamicResource CNName}" />
                            <TextBox
                                Grid.Row="2"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.CNName, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="3"
                                Grid.Column="1"
                                Text="{DynamicResource ENName}" />
                            <TextBox
                                Grid.Row="3"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.ENName, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="4"
                                Grid.Column="1"
                                Text="{DynamicResource VNName}" />
                            <TextBox
                                Grid.Row="4"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.VNName, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="5"
                                Grid.Column="1"
                                Text="{DynamicResource PhysicalLocation}" />
                            <TextBox
                                Grid.Row="5"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.PhysicalLocation, UpdateSourceTrigger=PropertyChanged}" />
                            <CheckBox
                                Grid.Row="6"
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                Content="{DynamicResource ReverseEnable}"
                                IsChecked="{Binding CurrentSelectIOInput.ReverseEnable}" />
                            <CheckBox
                                x:Name="ShielEnable"
                                Grid.Row="7"
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                Content="{DynamicResource ShieldEnable}"
                                IsChecked="{Binding CurrentSelectIOInput.ShieldEnable}" />
                            <TextBlock
                                Grid.Row="7"
                                Grid.Column="1"
                                Text="{DynamicResource ShieldEnableDefaultValue}" />
                            <CheckBox
                                Grid.Row="7"
                                Grid.Column="2"
                                Content="{DynamicResource ShieldDefaultValueInfo}"
                                IsChecked="{Binding CurrentSelectIOInput.ShiedlEnableDefaultValue}"
                                IsEnabled="{Binding ElementName=ShielEnable, Path=IsChecked}" />
                            <TextBlock
                                Grid.Row="8"
                                Grid.Column="1"
                                Text="{DynamicResource Group}" />
                            <ComboBox
                                Grid.Row="8"
                                Grid.Column="2"
                                Margin="5,0,0,0"
                                HorizontalContentAlignment="Center"
                                IsEditable="True"
                                ItemsSource="{Binding IOInputGroupCollection}"
                                Text="{Binding CurrentSelectIOInput.Group}" />
                            <TextBlock
                                Grid.Row="9"
                                Grid.Column="1"
                                Text="{DynamicResource Backup}" />
                            <TextBox
                                Grid.Row="9"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.Backup, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock
                                Grid.Row="10"
                                Grid.Column="1"
                                Text="Tag" />
                            <TextBox
                                Grid.Row="10"
                                Grid.Column="2"
                                Text="{Binding CurrentSelectIOInput.Tag, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </ScrollViewer>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="2,2,0,0"
                        Background="{StaticResource AppleHeavyBackGroundBrush}">
                        <Border
                            Margin="4"
                            BorderBrush="{StaticResource ApplelHeavyTextForegroundBrush}"
                            BorderThickness="1"
                            CornerRadius="5">
                            <ListBox ItemsSource="{Binding MessageList}">
                                <ListBox.ContextMenu>
                                    <ContextMenu DataContext="{Binding Data, Source={StaticResource BindingProxy}}">
                                        <MenuItem
                                            Command="{Binding MsgClearCommand}"
                                            Header="{DynamicResource ClearLog}"
                                            Icon="{materialDesign:PackIcon Kind=Close}" />
                                        <MenuItem
                                            Command="{Binding OpenLogDirectoryCommand}"
                                            Header="{DynamicResource OpenLog}"
                                            Icon="{materialDesign:PackIcon Kind=FolderSearchOutline}" />
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                                <i:Interaction.Behaviors>
                                    <listboxEx:ListBoxScrollToBottomExtensions />
                                </i:Interaction.Behaviors>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Time, StringFormat={}{0:HH:mm:ss:}}" />
                                            <TextBlock
                                                Margin="5,0,0,0"
                                                Text="{Binding Text}"
                                                TextWrapping="Wrap" />
                                        </DockPanel>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="INFO">
                                                <Setter Property="ListBoxItem.Foreground" Value="Black" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="DEBUG">
                                                <Setter Property="ListBoxItem.Foreground" Value="Black" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="WARN">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageOrangeBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="ERROR">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageRedBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Degree}" Value="FATAL">
                                                <Setter Property="ListBoxItem.Foreground" Value="{StaticResource MessageRedBrush}" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Border>
                </Grid>

            </Grid>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</UserControl>
