<UserControl
    x:Class="SpinCoaterAndDeveloper.App.Views.OptionFunctionShieldView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vc="clr-namespace:ValueConverters;assembly=ValueConverters"
    xmlns:viewmodels="clr-namespace:SpinCoaterAndDeveloper.App.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:OptionFunctionShieldViewModel}"
    d:DesignHeight="700"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <vc:BoolToBrushConverter
            x:Key="BoolToBrushConverter"
            FalseValue="#DD8E94"
            TrueValue="#87BC9E" />
    </UserControl.Resources>
    <Grid>
        <ItemsControl ItemsSource="{Binding FunctionShieldStatusCollection}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="2"
                        BorderBrush="{Binding IsActive, Converter={StaticResource BoolToBrushConverter}}"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Border
                            Background="{Binding IsActive, Converter={StaticResource BoolToBrushConverter}}"
                            BorderThickness="1"
                            CornerRadius="5">
                            <TextBlock
                                Padding="2,0,2,0"
                                HorizontalAlignment="Center"
                                FontSize="12"
                                Foreground="White"
                                Text="{Binding ShowOnUIName}" />
                        </Border>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
