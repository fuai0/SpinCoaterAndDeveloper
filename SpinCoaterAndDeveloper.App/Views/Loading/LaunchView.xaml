<UserControl
    x:Class="SpinCoaterAndDeveloper.App.Views.LaunchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:Height="400"
    d:Width="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="AllowsTransparency" Value="True" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="Width" Value="800" />
            <Setter Property="Height" Value="400" />
            <Setter Property="Title" Value="LaunchWindow" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid Background="White">
        <Canvas>
            <Image
                Canvas.Left="730"
                Canvas.Top="10"
                Width="60"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Source="pack://application:,,,/Resources/Image/CowainLogo5.png" />
            <Image
                Width="425"
                Source="/Resources/Image/cetc.jpg" HorizontalAlignment="Center" VerticalAlignment="Top" Height="166" Canvas.Left="304" Canvas.Top="47" >

            </Image>
            <Image
                Canvas.Left="340"
                Canvas.Top="240"
                Width="450"
                Source="pack://application:,,,/Resources/Image/CowainLogo2.png" />
            <Image
                Canvas.Top="176"
                Width="330"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Source="pack://application:,,,/Resources/Image/CowainLogo6.png" />
            <ProgressBar
                Canvas.Left="355"
                Canvas.Top="313"
                Width="435"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Foreground="#194781"
                IsIndeterminate="True" />
            <Grid
                Canvas.Left="355"
                Canvas.Top="330"
                Width="435">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    d:Text="AABBCCDD"
                    FontSize="30"
                    FontWeight="Bold"
                    Foreground="{StaticResource AppleLightTextForegroundBrush}"
                    Text="{Binding LaunchInfo}" />
            </Grid>
            <!--<ProgressBar
                Canvas.Left="200"
                Canvas.Top="148"
                Width="75"
                Foreground="#194781"
                IsIndeterminate="True"
                Style="{StaticResource MaterialDesignCircularProgressBar}"
                Value="0" />-->
        </Canvas>
    </Grid>
</UserControl>
