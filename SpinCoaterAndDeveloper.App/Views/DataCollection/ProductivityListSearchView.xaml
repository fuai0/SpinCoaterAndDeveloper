<UserControl
    x:Class="SpinCoaterAndDeveloper.App.Views.ProductivityListSearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="700"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Background="{StaticResource AppleHeavyBackGroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <UniformGrid Margin="0,5,0,0" Columns="10">
            <DatePicker
                Margin="5,0,0,0"
                Padding="0"
                VerticalAlignment="Center"
                materialDesign:HintAssist.Hint="{DynamicResource SelectStartDate}"
                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                Text="{Binding ProductivityStartDate, Mode=TwoWay}" />
            <DatePicker
                Margin="10,0,0,0"
                Padding="0"
                VerticalAlignment="Center"
                materialDesign:HintAssist.Hint="{DynamicResource SelectEndDate}"
                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                Text="{Binding ProductivityEndDate, Mode=TwoWay}" />
            <Button
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Command="{Binding SearchProductivityCommand}"
                CommandParameter="ListShow"
                Content="{DynamicResource Search}" />
        </UniformGrid>
        <DataGrid
            Grid.Row="1"
            Margin="5,0,5,0"
            materialDesign:DataGridAssist.CellPadding="20 2 2 2"
            materialDesign:DataGridAssist.ColumnHeaderPadding="20 2 2 2"
            materialDesign:DataGridAssist.SelectedCellBorderBrush="Transparent"
            AutoGenerateColumns="False"
            Background="{StaticResource AppleHeavyBackGroundBrush}"
            CanUserAddRows="False"
            CanUserSortColumns="False"
            HeadersVisibility="All"
            IsReadOnly="True"
            ItemsSource="{Binding ProductivityListCollections}"
            RowHeaderWidth="1"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Time}"
                    Header="{DynamicResource Time}" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding TotalNums}"
                    Header="{DynamicResource Productivity}" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FailNums}"
                    Header="{DynamicResource FailNums}" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Percentage}"
                    Header="{DynamicResource Percentage}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
